{
  "name": "csv-helpers",
  "version": "0.0.2",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/js-csv-helpers.js --minify",
    "watch": "esbuild src/index.js --bundle --outfile=dist/js-csv-helpers.js --minify --watch",
    "test": "jest",
    "pub": "npm version patch --force && npm publish --access=public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@jrc03c/js-math-tools": "^0.0.22",
    "papaparse": "^5.3.2"
  },
  "devDependencies": {
    "@jrc03c/make-key": "^0.0.6",
    "cypress": "^12.6.0",
    "esbuild": "^0.17.10",
    "jest": "^29.4.3"
  }
}
